---
layout: null
permalink: /feed.xml
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ "/" | absolute_url }}</id>
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author>
    <name>{{ site.social.name | xml_escape }}</name>
    <uri>{{ "/" | absolute_url }}</uri>
  </author>
  <link rel="self" type="application/atom+xml" href="{{ page.url | absolute_url }}"/>
  <link rel="alternate" type="text/html" href="{{ '/' | absolute_url }}"/>
  <generator uri="https://jekyllrb.com/">Jekyll</generator>

{% for post in site.posts limit: 10 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    <content type="text/html" xml:base="{{ post.url | absolute_url }}">{{ post.content | xml_escape }}</content>
    
    {% if post.categories %}
      {% for category in post.categories %}
        <category term="{{ category | xml_escape }}" />
      {% endfor %}
    {% endif %}

    {% if post.tags %}
      {% for tag in post.tags %}
        <category term="{{ tag | xml_escape }}" />
      {% endfor %}
    {% endif %}

    <summary>{{ post.excerpt | strip_html | truncate: 300 | xml_escape }}</summary>
  </entry>
{% endfor %}
</feed>
