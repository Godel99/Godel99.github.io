---
title: "[Platinum III] 트리의 MEX - 25549"
date: 2026-04-19
categories: [PS, BOJ] # BOJ, Programmers
tags: [자료 구조, 트리, 집합과 맵, 트리를 사용한 집합과 맵, 작은 집합에서 큰 집합으로 합치는 테크닉]
---
[문제 링크](https://www.acmicpc.net/problem/25549) 
## **풀이**
0을 포함한 양의 정수값을 $N$개 정점으로 하는 트리에서 $i$번 정점을 루트로 하는 서브 트리의 정점에 적혀 있지 않은 수 중에서 가장 작은 음이 아닌 정수를 $mex(i)$라고 합니다. $1$이상 $N$이하의 모든 정수 $i$에 대해 $mex(i)$를 구해야 합니다.

### **집합과 트리**
$mex(i)$는 트리의 노드마다 정수값을 포함하는 **집합**으로 표현할 수 있습니다. 집합은 중복을 무시하거나 빠르게 값을 찾을 때 좋습니다. **부모의 $mex(p)$는 자식 $mex(c)$보다 항상 크므로** 부모 노드의 집합이 자식 노드의 집합을 포함합니다. 그러므로 루트 노드부터 리프 노드까지 내려가서 올라오며 $mex$를 확인해 줘야 합니다. 리프 노드까지는 **DFS**이나 **위상 정렬+반복**을 써서 도달하면 됩니다.

### **Small-to-Large 병합**
리프 노드부터 올라오면서 서브 트리 간에 병합을 해야 합니다. 집합의 크기가 더 큰 쪽이 작은 쪽을 병합하는 게 더 빠릅니다. 왜냐면 작은 쪽 데이터가 더 적기 때문입니다.이를 **Small-to-Large 병합**이라고 합니다. 

## **구현**
<details markdown="1">
<summary>코드</summary>

```python
import sys
input = lambda: sys.stdin.readline().rstrip()
print = lambda *args, sep=" ", end="\n": sys.stdout.write(sep.join(map(str, args)) + end)

def main():
    n = int(input())
    p = [0]+[*map(int, input().split())]
    e = [[] for _  in range(n+1)]
    s = [set() for _ in range(n+1)]
    v = [0]+[*map(int, input().split())]
    deg = [0]*(n+1)
    for i in range(1, n+1):
        if p[i] == -1: continue
        deg[p[i]] += 1
        e[p[i]].append(i)
    st = [i for i in range(1, n+1) if deg[i] == 0]
    ans = [0]*n
    while st:
        cur = st.pop()
        s[cur].add(v[cur])
        prv = 0
        for nxt in e[cur]: prv = max(prv, ans[nxt-1])
        for nv in range(prv, n+1):
            if nv not in s[cur]:
                ans[cur-1] = nv
                break
        if p[cur] == -1: continue
        if len(s[p[cur]]) >= len(s[cur]): s[p[cur]].update(s[cur])
        else:
            s[cur].update(s[p[cur]])
            s[p[cur]] = s[cur]
        deg[p[cur]] -= 1
        if deg[p[cur]] == 0: st.append(p[cur])
    print('\n'.join(map(str, ans)))
    return
if __name__ == "__main__":
    main()
```
```c++
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0); cout.tie(0); ios::sync_with_stdio(false);
    int n; cin >> n;
    vector<int> p(n+1), v(n+1), ans(n);
    vector<vector<int>> e(n+1);
    vector<set<int>> s(n+1);
    int root = -1;
    for(int i = 1; i <= n; i++){
        cin >> p[i];
        if(p[i] == -1) root = i;
        else e[p[i]].push_back(i);
    }
    for(int i = 1; i <= n; i++) cin >> v[i];
    function<void(int)> dfs = [&](int cur){
        s[cur].insert(v[cur]);
        int prv = 0;
        for(int nxt : e[cur]){
            dfs(nxt);
            prv = max(prv, ans[nxt-1]);
            if(s[nxt].size() > s[cur].size()) s[cur].swap(s[nxt]);
            for(int nv : s[nxt]) s[cur].insert(nv);
        }
        while(s[cur].count(prv)) prv++;
        ans[cur-1] = prv;
    };
    dfs(root);
    for(int i : ans) cout << i << '\n';
    return 0;
}
```
</details>

### 시간 및 공간 복잡도
* 시간복잡도: $\mathcal{O}(N \log^2 N)$
* 공간복잡도: $\mathcal{O}(N)$
