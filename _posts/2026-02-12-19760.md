---
title: "[Gold I] Операция <<Перестановка>> - 19760"
date: 2026-02-12
categories: [PS, BOJ] # BOJ, Programmers
tags: [그래프 이론, 그래프 탐색, 깊이 우선 탐색, 방향 비순환 그래프, 위상 정렬]
---
[문제 링크](https://www.acmicpc.net/problem/19760) 
## **풀이**
### 문제 요약
길이 $N$의 순열 $A$에 관한 $M$개의 정보가 순서대로 주어집니다. 각 정보는 $x$, $y$ 꼴로 주어지며, 이는 $A[x]<A[y]$를 의미합니다. 

몇 번째 정보까지 주어졌을 때 처음으로 $A$를 유일하게 결정할 수 있게 되는지 구하세요.

### 접근
$A[x]<A[y]$를 간선 $x\rightarrow y$로 치환하면 **그래프+위상 정렬** 문제가 됩니다. 결국 유일한 위상 정렬이 완성되는 **시점**을 찾아야 하는데 위상 정렬이 이뤄질 때마다 카운터 했다간 $k$가 정말 유일한지를 또 검사해 줘야 해서 복잡해집니다.

### 시점
중요한 것은 유일한 간선이 만들어지는 **시점**이니 간선을 추가할 때 그 시점을 $idx$에 기록해 둡시다.

### 유일한 간선 여부
**진입 차수가 0인 것이 2개 이상**이면 여러 가지 간선이 있다는 뜻이므로, 유일한 간선을 결정할 수 없습니다.

### 시점 결정
진입 차수가 0이 되는 노드는 간선이 결정되고 간선이 결정되는 시점은 노드마다 다 다릅니다. 

간선이 결정될 때마다 **가장 늦게 결정된(가장 높은 $idx$)** 노드의 시점을 기록해서 가장 늦은 시점을 찾아줍니다.

## **구현**
<details markdown="1">
<summary>코드</summary>

```python
import sys
def print(*args, sep=" ", end="\n"): return sys.stdout.write(sep.join(map(str, args)) + end)
def input(): return sys.stdin.readline().rstrip()

from collections import deque

def main():
    n, m = map(int, input().split())
    deg = [0]*(n+1)
    e = [[] for _ in range(n+1)]
    for i in range(1, m+1):
        x, y = map(int, input().split())
        e[x].append((y, i))
        deg[y] += 1
    dq = deque()
    for i in range(1, n+1):
        if not deg[i]: dq.append(i)
    ans = 0
    while dq:
        if len(dq) > 1: return int(not print(-1))
        cur = dq.popleft()
        for nxt, idx in e[cur]:
            deg[nxt] -= 1
            if not deg[nxt]: 
                dq.append(nxt)
                ans = max(ans, idx)
    print(ans)
    return 0
if __name__ == '__main__':
    sys.exit(main())
```
```c++
#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

int main(){
    cin.tie(0); cout.tie(0); ios::sync_with_stdio(false);
    int n, m; cin >> n >> m;
    vector<vector<pii>> e(n+1);
    vector<int> deg(n+1);
    for(int i = 1; i <= m; i++){
        int x, y; cin >> x >> y;
        e[x].push_back({y, i});
        deg[y]++;
    }
    queue<int> q;
    for(int i = 1; i <= n; i++) if(!deg[i]) q.push(i);
    int ans = 0;
    while(q.size()){
        if(q.size() > 1) return !(cout << -1);
        int cur = q.front(); q.pop();
        for(auto [nxt, idx] : e[cur]){
            deg[nxt]--;
            if(!deg[nxt]){
                q.push(nxt);
                ans = max(ans, idx);
            }
        }
    }
    cout << ans;
    return 0;
}
```
</details>

### 시간 및 공간 복잡도
* 시간복잡도: $\mathcal{O}(N+M)$
* 공간복잡도: $\mathcal{O}(N+M)$
