---
title: "[Platinum V] 나는 뱀파이어 - 33626"
date: 2026-03-06
categories: [PS, BOJ] # BOJ, Programmers
tags: [그래프 이론, 그리디 알고리즘, 그래프 탐색, 트리, 이분 탐색, 깊이 우선 탐색, 매개 변수 탐색]
---
[문제 링크](https://www.acmicpc.net/problem/33626) 
## **풀이**
트리 구조에서 $P$가 루트 노드이며, 뱀파이어는 아래에서 위로만 움직일 수 있습니다. 그러므로 뱀파이어는 가장 아래 **리프 노드**에서 시작하는 게 최선이라는 것은 자명합니다.

### **관점 전환**
$M$ 마리의 뱀파이어로 모든 학생을 감염시키는 최소 시간을 구하는 것이 목표이지만, 관점을 바꿔서 시간을 $T$ 초로 제한했을 때, $M$ 마리 이하의 뱀파이어만으로 모든 학생을 감염시킬 수 있는가로 질문을 바꿔봅니다.

### **단조성: 매개 변수 $T\rightarrow$ 이분 탐색**
**시간 $T$** 를 기준으로 잡는다면, 시간이 길다면 뱀파이어 수가 더 적을 수 있고 시간이 짧다면 뱀파이어 수가 늘 수 밖에 없습니다. 이러한 단조성이 유지된다면 시간을 기준으로 **이분 탐색**을 실시해 연산을 줄일 수 있습니다.

### **그리디(Greedy)**
가장 아래 리프 노드에 뱀파이어를 하나 세워두고 시간 $T$ 까지 위로 움직입니다. 만약 루프 노드까지 도달하지 못했다면, 그 노드부터 다시 뱀파이어를 추가하는 겁니다. 리프 노드부터 시간 $T$ 마다 뱀파이어를 추가해 리프 노드까지 도달한다면, 최적의 뱀파이어 수를 찾는 **그리디** 전략입니다.

시간 $T$ 마다 뱀파이어를 추가해 뱀파이어 수가 $M$을 초과하거나 부족하다면 이를 기준으로 시간 $T$ 도 늘리거나 줄여가며 **최적의 시간 $T$** 를 찾아갑니다.

### **DFS 깊이 우선 탐색**
루트 노드부터 가장 아래 리프 노드까지 도달하는 과정은 **DFS**를 쓰면 편리합니다. 리프 노드에 도달 후에는 다시 **올라오면서** 뱀파이어를 시간 $T$ 마다 추가하는 방법입니다.

계획 시작 시 시간이 1이고 실제로 DFS로 구한 것은 이동 거리이므로 결과에 $+1$ 해줘야 합니다.

## **구현**
<details markdown="1">
<summary>코드</summary>

```python
import sys 
print = lambda *args, sep=" ", end="\n": sys.stdout.write(sep.join(map(str, args)) + end)
input = lambda: sys.stdin.readline().rstrip('\r\n')

sys.setrecursionlimit(200000)

def main():
    n, m, p = map(int, input().split())
    e = [[] for _ in range(n+1)]
    for _ in range(n-1):
        u, v = map(int, input().split())
        e[u].append(v)
        e[v].append(u)
    def dfs(cur, pre, t):
        nonlocal cnt
        ret = t+1
        for nxt in e[cur]:
            if nxt == pre: continue
            ret = min(ret, dfs(nxt, cur, t)+1)
        if ret > t and cur != p:
            ret = 0
            cnt += 1
        return ret
    l, r = 0, n
    while l < r:
        mid = l+r>>1
        cnt = 0
        dfs(p, 0, mid)
        if cnt <= m: r = mid
        else: l = mid+1
    if l == n: print(-1)
    else: print(l+1)
if __name__ == '__main__':
    main()
```
```c++
#include<bits/stdc++.h>
using namespace std;
using ll = long long;
vector<vector<int>> e;
int cnt, p;

int dfs(int cur, int pre, int t){
    int ret = t+1;
    for(auto nxt : e[cur]){
        if(nxt == pre) continue;
        ret = min(ret, dfs(nxt, cur, t)+1);
    }
    if(ret > t && cur != p){
        ret = 0;
        cnt++;
    }
    return ret;
}

int main(){
    cin.tie(0); cout.tie(0); ios::sync_with_stdio(false);
    int n, m; cin >> n >> m >> p;
    e.resize(n+1);
    for(int i = 1; i < n; i++){
        int u, v; cin >> u >> v;
        e[u].push_back(v);
        e[v].push_back(u);
    }
    int l = 0, r = n;
    while(l < r){
        int mid = l+r>>1;
        cnt = 0;
        dfs(p, 0, mid);
        if(cnt <= m) r = mid;
        else l = mid+1;
    }
    if(l == n) cout << -1;
    else cout << l+1;
    return 0;
}
```
</details>

### 시간 및 공간 복잡도
* 시간복잡도: $\mathcal{O}(N \log N)$
* 공간복잡도: $\mathcal{O}(N)$
